# ClearSight Project Plan

## Project concept
ClearSight is a static, privacy-first accessibility utility that helps designers and developers **see** and **fix** accessibility issues in UI screenshots fast. The app lets users upload a screenshot (or use built-in demo scenes), then generates side-by-side visualizations for common vision deficiencies and low-vision conditions. It also includes a WCAG contrast checker that can validate text/background pairs and suggest accessible alternatives.

The project is intentionally scoped for a one-day build:
- One click to load an image.
- Immediate client-side simulation in-browser.
- Immediately understandable, demo-friendly output.
- Lightweight export and sharing workflow to support hackathon judging videos.

## Chosen tech stack
- **Frontend**: HTML5, CSS3, Vanilla JavaScript (ES modules)
- **Image processing**: Canvas API
- **Accessibility logic**: WCAG contrast ratio math (relative luminance + ratio calculation) implemented in JS
- **Testing**: Node.js built-in `node --test` for pure utility functions, plus `npm run smoke` (puppeteer-core + headless Chrome) for end-to-end workflow verification against the production bundle
- **Hosting**: GitHub Pages from `/docs` directory with relative asset paths

## Feature list (priority order)
### MVP (must-have)
1. Image upload or built-in demo image loading.
2. Render a source preview plus multiple simulation cards in-browser using Canvas.
3. Simulate common color-vision deficiency modes, with clear labels and loading/error states.
4. Add two low-vision modes (blur and low contrast) for visual accessibility simulation.
5. WCAG contrast checker with explicit pass/fail result for AA/AAA thresholds.
6. Visible error and validation messages for unsupported files, oversized images, and invalid colors.

### High-priority enhancements
7. Palette suggestion panel to propose accessible replacement color pairs.
8. One-click copy of suggested palette values and re-run check.
9. Add a real-time render progress rail with per-mode completion feedback.
10. Add automatic large-image import downscaling to keep rendering smooth for high-res screenshots (5kpx render cap, local-only workflow).

### Nice-to-have
10. Instructional demo script links and screenshot checklist for Devpost submission.
11. Lightweight export controls for generated results (downloadable previews).
12. Add source-image color picker controls for direct contrast text/background sampling.
13. Add discoverable keyboard shortcut help panel with `?`/`H` toggle and shortcut legend.
14. Add judge-ready markdown summary export for human-readable accessibility review and submission handoff.
15. Add interactive workflow onboarding checklist that auto-updates as users complete image load, render, and contrast-check milestones.
16. Clarify the 90-second judge workflow and in-app onboarding copy for faster first-pass success.

## Session checkpoint (2026-07-22)
- [x] Make every shared project link **pitch-ready before the app even opens**: replaced the generic simulator title/description with the screenshot → repair → verified-proof promise; added canonical GitHub Pages, Open Graph, and Twitter Card metadata; designed a bespoke 1200×630 social preview that truthfully mirrors the built-in audit (79 → 81 verified repair, remaining text/target risks, 12 simulations, six axes); added descriptive image alt metadata; cached the card for offline installs; pinned the full deployment metadata contract in browser smoke; offline cache bumped to v60; production `docs/` regenerated.
- [x] Replace the **feature-wall default experience with progressive disclosure and a judge-focused evidence dock**: the specialist WCAG 2.3.1 motion scanner and WCAG 2.4.7/2.4.13 keyboard-focus lab now present as compact, accessible disclosure cards with plain-language capability summaries, preserving every advanced control and result while removing two large empty analysis canvases from the first-pass journey. The simulations section now leads with four high-value handoff artifacts (submission ZIP, verdict card, PDF report, and repair proof), moves secondary preview/data/video/compliance formats behind a clearly labeled disclosure, and separates view controls from exports. Keyboard focus states, responsive mobile layouts, and smoke-test expansion of specialist labs are covered; offline cache bumped to v59; production `docs/` regenerated.
- [x] Rebuild the **judge-facing front door around ClearSight's closed-loop proof**: the previous hero read like a generic vision simulator and hid the project's most differentiated capability below a dense mission-control panel. The new responsive split hero leads with the screenshot → detect → repair → verified-proof promise, adds a one-click 90-second audit CTA wired to the real guided workflow, keeps direct upload navigation prominent, and shows a truthful miniature of the built-in demo's measured 79 → 81 repair result (including remaining text and target risks so the preview never implies a perfect fix). Local-processing, WCAG 2.2 evidence, and exportable-proof guarantees now land before the first scroll; desktop and 390px mobile renders were visually verified; browser smoke pins the promise and enabled CTA; offline cache bumped to v58; production `docs/` regenerated.
- [x] Close the **sixth-axis developer handoff gap for WCAG 2.5.8 tap targets**: the CSS fix sheet previously shipped verified text and component colors but omitted the layout failures that already affected the score and release gate. `buildCssFixSheet()` now accepts validated target fixes and emits one screenshot-localized `.clearsight-target-fix-N` template per crowded undersized control with its measured dimensions, 24×24 CSS px minimum hit-area rules, a spacing-exception alternative, and an explicit reminder that the placeholder must be mapped to the real source selector (never claiming a screenshot can infer DOM selectors). Target-only audits now unlock Copy/Download CSS, live payload construction carries every target finding, descriptions/counts include the sixth axis, the submission ZIP inherits the richer sheet automatically, unit and browser-smoke contracts pin the generated rules and honesty copy, README/in-app handoff copy is updated, offline cache bumped to v57, and production `docs/` regenerated.
- [x] Ship a **verified accessibility repair proof card** that turns ClearSight's closed-loop remediation into a pitch-ready claim: after any pixel repair is re-audited, a judge-gated 1200×630 PNG unlocks with the exact preserved pre-repair screenshot beside the verified result, ClearSight Score + grade delta, before→after counts for below-AA text / invisible surfaces / undersized targets / hidden CVD risks, repair kind, changed-pixel provenance, and an explicit on-device six-axis verification line. The artifact is automatically included in the submission ZIP and manifest only when real repair baseline evidence exists (never for an untouched audit), with 3 new browser smoke assertions, README workflow docs, offline cache v56, and production `docs/` regenerated.
- [x] Make the new **Focus order map a first-class evidence artifact** instead of a live-demo-only visual: once any tab-through yields a mapped stop, the panel unlocks a direct **Download focus map PNG** action; the exact numbered baseline overlay (green/amber WCAG 2.4.13 badges, stop outlines, and joined tab path) is automatically added to the submission ZIP as `<source>-focus-order-map.png` and listed in its manifest as WCAG 2.4.7/2.4.13 evidence. Empty/reset state keeps the action disabled, browser smoke pins a real PNG payload plus the manifest contract (159 smoke checks), README documents the handoff workflow, offline cache bumped to v55, and production `docs/` regenerated.
- [x] Ship the **Focus order map (WCAG 2.4.7 / 2.4.13 across the whole tab journey)** — the two-frame focus check proves one focus state; a short screen recording now proves the entire keyboard journey. New pure fold-based tracker in vision-core (`createFocusSequenceTracker()` / `trackFocusSequenceFrame()` / `summarizeFocusSequence()` + batch wrapper `analyzeFocusSequence()` + `FOCUS_SEQUENCE_DEFAULTS`/`FOCUS_SEQUENCE_LABELS`): every sampled frame is diffed against the unfocused baseline via the existing `analyzeFocusIndicator()`, so each frame's changed region is exactly the indicator of wherever focus sits at that moment. Honest classifications per frame: `new-stop` (numbered in tab order with its own full 2.4.13 measurement), `same-stop` (deduped, keeping the best-measured capture because mid-transition frames under-measure their own ring), `revisit` (Tab cycling back is counted, not duplicated), `no-indicator`, `view-changed` (diff bbox covering >50% of the frame = scroll/navigation, set aside instead of misread as focus), and `overflow` past the 24-stop cap. Aggregate verdict = none/weak/strong with `worstStopOrder`. The tracker is a true fold — only the baseline stays in memory, so the in-app video path (MP4/WebM/MOV via the existing seek-sampling plumbing, 3 fps, ≤90 frames, ≤1280px) never accumulates decoded frames. In-app "Focus order map" block in card 1d: video loader + built-in demo that synthesizes a 7-frame tab-through of the demo UI (strong ring on Create report, faint ring on the ghost search input, strong ring on Share, plus idle/duplicate/revisit frames → 3 stops, 2 strong / 1 weak, every skip category exercised), verdict banner, 5 stat tiles, an overlay painting the numbered tab path on the dimmed baseline (green badge = meets 2.4.13, amber = below, dashed path joining stops, per-stop bounding boxes), ordered per-stop list with measured areas/contrast/indicator colors, per-verdict guidance; state clears on workspace reset. Report JSON (`focusSequence`), judge-summary "Focus order map" line, remediation actions (high for no-indicator recordings, medium naming the worst stop), `runFocusSequenceDemo`/`getFocusSequence` hooks. `buildWcagConformanceSummary` now folds the sequence into the 2.4.7/2.4.13 rows with worst-verdict-wins semantics when both a pair and a sequence exist (one strong focused screenshot cannot excuse a faint indicator elsewhere in the tab order; a baseline-only sequence with zero analyzed frames is honestly ignored), and either kind of focus evidence trims 2.4.7 from the manual list. 4 new unit tests (163 total: full map/dedupe/revisit pipeline with exact ring geometry, view-change + maxStops + best-capture-upgrade + retina cssPixelRatio, conformance worst-verdict folding incl. pair-vs-sequence in both directions, validation). 6 new smoke checks (158 total) drive the real demo button end-to-end: weak aggregate verdict, exact summary counts, stat tiles + ordered stop list + status line, pixel-verified green/amber badges on the painted overlay, report/summary/remediation embeds, and the live worst-verdict-wins conformance override of the earlier strong pair demo. README (#15 + conformance copy) documented; SW cache bumped to v54; production `docs/` regenerated.
- [x] Complete the above-the-fold **six-axis mission-control verdict**: the command center previously displayed the real six-axis score/radar but silently omitted WCAG 1.4.11 component-surface failures and WCAG 2.5.8 undersized targets from its metric tiles, headline risk count, evidence summary, and next-action routing. It now shows seven responsive evidence tiles (all six finding axes plus peak simulation shift), counts the demo's invisible ghost surface and three crowded targets in the live verdict, states how many text regions / palette pairs / component surfaces / tap targets were actually measured, and routes surface findings to the one-click repair while giving target findings an honest layout-change action. Fixed the stale five-axis placeholder copy, extended browser smoke coverage to pin the complete mission-control contract, bumped the offline cache to v53, and regenerated production `docs/`.
- [x] Ship the **two-frame Focus appearance check (WCAG 2.4.7 / 2.4.13)** — the first tool-side answer to a criterion every screenshot auditor must punt to manual testing: a focus indicator only exists while focus is present, so one screenshot can never prove 2.4.7 Focus Visible, but two can. New pure `analyzeFocusIndicator(baseData, focusData, w, h, opts)` + `FOCUS_APPEARANCE_DEFAULTS`/`FOCUS_APPEARANCE_LABELS` in vision-core diff the unfocused vs focused frames (Lab ΔE ≥ 6 with a cheap channel prefilter, sub-threshold compression noise ignored), then judge the changed region against the real 2.4.13 metrics: per-pixel WCAG contrast between the focused and unfocused states (≥3:1 bar), contrasting area vs a 1-CSS-px perimeter of the component (bounds honestly approximated by the indicator's bounding box, stated in the evidence), `cssPixelRatio` for retina pairs, dominant indicator/base colors via quantized histogram, and a 1/2-valued mask for overlay painting. Three honest tiers: `none` (2.4.7 Does Not Support), `weak` (visible in a diff ≠ visible to a user → Partially Supports 2.4.7, fails 2.4.13), `strong` (both bars met). `buildWcagConformanceSummary` now consumes `report.focusCheck`: measured 2.4.7 + 2.4.13 rows join the machine-checked table and 2.4.7 leaves the manual-testing list (returned as dynamic `manualCriteria`; reports without a pair are byte-identical to before). In-app card "1d) Focus appearance check": load unfocused/focused frames (same-size validation, ≤2400px identical downscale), two built-in demos diffing the demo UI against itself — faint 2px #d8e0ea ring = 1,736 changed px / 0 at 3:1 → weak; bold 6px #1d4ed8 ring = 5,304 contrasting px vs 904 px² minimum, 6.41:1 → strong — verdict banner, 5 stat tiles, dimmed focused-frame overlay with magenta changed/contrasting pixels, per-verdict guidance; state clears on workspace reset. Report JSON (`focusCheck`), judge-summary snapshot line, remediation actions (high for none, medium for weak), `runFocusDemo`/`getFocusCheck` hooks. `createDemoImage` refactored to shared `paintDemoScene()`. 4 new unit tests (159 total) incl. exact ring-geometry pixel counts, retina scaling, noise rejection, and conformance-row/manual-list assertions; 6 new smoke checks (150 total) driving both real demo buttons end-to-end through report/summary/remediation/conformance and pixel-verifying the painted overlay. README (how-it-works #14 + conformance copy) documented; SW cache bumped to v52; production `docs/` regenerated.
- [x] Ship a **presentation-ready audit verdict card** so ClearSight's technical depth lands in one glance during judging: a new judge-gated export builds a polished 1200×630 PNG from the live report with the grade-colored ClearSight Score ring, all six scored axes, the audited screenshot, measured counts for below-AA text / hidden CVD failures / invisible component surfaces / undersized targets, the highest-priority remediation action, and an explicit private/on-device proof line. The card is downloadable directly, automatically bundled into the submission ZIP, and named in its manifest; it never invents findings or uploads pixels. Added two end-to-end smoke assertions for the real painted PNG and manifest contract, documented the pitch-deck/Devpost workflow, bumped the offline cache to v51, and regenerated production `docs/`.
- [x] Ship **scan-to-phone QR share codes** built by a from-scratch QR encoder — the shareable audit link now jumps to a judge's phone with one camera scan, zero server. New pure `encodeQrMatrix()` + `qrReedSolomonEncode()` + `qrFormatInfoBits()` + `qrVersionInfoBits()` + `qrBlockStructure()` + `QR_ENCODE_DEFAULTS` in vision-core implement ISO/IEC 18004 byte-mode encoding end to end: GF(256) Reed-Solomon EC (poly 0x11D), the full version 1–40 block-structure table for EC levels M and L (auto prefers M, falls back to L for large payloads, and a payload beyond V40-L's 2,953 bytes throws an honest capacity error instead of emitting a corrupt code), BCH(15,5) format info + BCH(18,6) version info, finder/timing/alignment placement (incl. the timing-vs-format reservation subtlety at (6,8)/(8,6) that a first pass got wrong and tests caught), zigzag data placement, and all 8 masks scored by the 4 penalty rules. Verified three independent ways: published spec vectors pinned in unit tests (HELLO WORLD 1-M Reed-Solomon EC bytes, format info L/mask-4, version info v7), a structural decoder in the test suite that reads format info back out of the matrix (validating its BCH code), unmasks, deinterleaves, recomputes every RS block, and byte-mode-parses payloads at small/medium/max-capacity sizes, and — during development — Apple's Vision framework decoding rendered PNGs of real share links at versions 5/20/40 in both EC levels byte-for-byte. In-app: judge-gated **Show QR share code** button renders the link into a canvas card (4-module quiet zone, pixelated scaling, white-on-black for scanner compatibility) with meta line (URL bytes, QR version, module count, EC level), **Download QR PNG**, and Hide; stale codes clear on re-render and workspace reset; `buildShareQr`/`getShareQrRaw` hooks. 5 new unit tests (155 total), 6 new smoke checks (real button click → painted card with spec finder/timing patterns in the live matrix, white quiet zone + dark finder pixels on canvas, version/size/byte consistency vs the real ~2.6 KB link, meta copy, hide-and-clear). README documents the encoder + verification story; SW cache bumped to v50; production `docs/` regenerated.
- [x] Add a live **six-axis accessibility risk fingerprint** to the above-the-fold ClearSight Score — an accessible radar visualization now exposes the shape behind the composite score (text contrast, CVD safety, APCA, color independence, component contrast, and tap-target size) and redraws after every audit or repair. The SVG includes a dynamically generated screen-reader description with every axis value, responsive mobile layout, and smoke coverage that pins all six plotted and narrated axes. Offline cache bumped to v49; production `docs/` regenerated.
- [x] Ship a **VPAT-style WCAG conformance summary export** — the audit now travels into compliance conversations as a formal document. New pure `buildWcagConformanceSummary(report)` + `buildConformanceStatementMarkdown(report, options)` + frozen `CONFORMANCE_OUTCOME_LABELS`/`CONFORMANCE_ADVISORY_LABELS`/`CONFORMANCE_MANUAL_CRITERIA` in vision-core map every machine-checkable WCAG 2.2 criterion to a VPAT outcome with the measured evidence beside it: 1.4.1 Use of Color (collision pairs w/ worst ΔE collapse + mode), 1.4.3/1.4.6 text contrast (below-AA/AAA counts + worst ratio/pair/position), 1.4.11 Non-text Contrast (failing surfaces + worst ratio), 2.3.1 Three Flashes (high→Does Not Support, caution→Partially Supports, static screenshot→honest Not Evaluated), 2.5.8 Target Size (undersized counts, spacing-exempt noted). Advisory table covers CVD-projected hidden failures + APCA false passes (Risk detected / No risk detected). The document is deliberately honest: "Supports" is defined as "no failures detected by automated pixel analysis of this screenshot — not a legal conformance claim", and a *Requires manual testing* section lists 6 DOM/interaction criteria (1.1.1, 1.3.1, 1.4.4, 2.1.1, 2.4.7, 4.1.2) with per-criterion reasons. Missing axes render Not Evaluated instead of guessing; markdown table cells are pipe-escaped; ClearSight Score headline + outcome counts included. In-app: judge-gated **Download conformance summary** button (title copy documents the contract), `<name>-conformance-summary.md` bundled into the submission package zip + manifest row + judge-snapshot expected outputs, and a `buildConformanceSummaryText` smoke hook. Also fixed stale "five audit axes" copy (complete-repair message + README undo paragraph) to six. 5 new unit tests (150 total: fully-failing report mapping w/ evidence regexes, clean report + caution→partial, empty report Not-Evaluated + validation throws, markdown structure/honest scope/manual list, pipe-escaping + overrides); 7 new smoke checks (137 total: live demo criterion rows incl. 1.25:1 ghost surface and 3-of-N undersized targets, static-screenshot 2.3.1 honesty, advisory evidence, manifest row, real download button message, and the post-repair re-audit flipping 1.4.11 to Supports while 1.4.3 stays failing). README + panel copy documented; SW cache bumped to v48; production `docs/` regenerated.
- [x] Fold **WCAG 2.5.8 tap-target sizing into the ClearSight Score, batch portfolio, and CI release gate**: `computeAccessibilityScore()` now exposes a sixth independently weighted axis (15 weight, renormalized when no target is measurable); `auditImageAccessibility()` runs the exact shared `scanTargetSizes()` detector headlessly and returns its measured targets/findings; the live score consumes the same result. Batch CSV, terminal tables, portfolio totals/PDF, GitHub Markdown, and SARIF now carry undersized-target counts, and a crowded target blocks the release gate instead of remaining an informational side panel. Added score validation and propagation coverage (145 unit tests total), updated six-axis product copy, bumped the offline cache to v47, and regenerated the production `docs/` bundle.
- [x] Add a **tap target size scan (WCAG 2.2 SC 2.5.8 Target Size — Minimum)** — a sixth static analysis surface catching the one failure no color lens can see: controls too small or too crowded to hit. New pure `scanTargetSizes()` + `TARGET_SIZE_DEFAULTS` in vision-core measures target geometry to pixel precision: regions with a resolved 1.4.11 component surface are flood-filled (Lab ΔE ≤ 4 — tighter than the boundary-departure ΔE 5, so a ghost surface within ΔE 6 of the page can't leak) and a flood that escapes the sampling window on BOTH axes is honestly rejected as "the page itself" (the march crossed whitespace to some other element) and re-analyzed; page-sitting regions are searched for compact solid ink blobs (icon buttons, chips) with solidity ≥ 0.55 + min/max-size filters so thin glyph strokes are never mistaken for tap targets, and window-edge-clipped blobs are skipped as untrustworthy fragments. Sizes convert to CSS px via `cssPixelRatio` (app passes working/original scale; 1× screenshot assumption stated in copy), and undersized targets only FAIL when a 24px circle centered on them intersects a neighboring target or another undersized target's circle — the official spacing-exception test procedure, honored honestly (`spacing-exempt` outcome). New "Tap target size (WCAG 2.5.8)" panel under the component scan lists failures with swatches, measured W×H vs 24×24, and clearance context; status names the spacing exception and states that the fix is a LAYOUT change no pixel repaint can honestly claim. Demo UI sample gained a crowded 16px slate icon toolbar (three #475569 squares, 6px gaps, two clean tile rows below the shrunk 230px-tall dark panel) tuned so every pinned contract is untouched (belowAA=1, cvdHidden=2, apcaFalse=0, collisions=3, component 7 evaluated/1 failing, score 79 Grade C — verified by live probe before/after) while adding exactly 3 undersized-crowded findings at ~15–17 CSS px; buttons/ghost-input/tile-cards measure as passing surfaces (e.g. Export 320.0×88.5 CSS px) proving the scan measures honestly rather than only flagging. Findings embed in report JSON (`targetSizes`), CSV (3 new `target_size_*` columns), judge summary snapshot line, and a high-priority remediation action citing 2.5.8 with the layout-change fix. 5 new unit tests (144 total: crowded-fail, spacing-exempt + 2× retina scaling, pixel-precise button measurement vs tile-quantized boxes, glyph rejection, validation) + 6 new smoke checks (130 total, incl. driving the real demo panel to 3 undersized findings and the summary/CSV/remediation embeds). NOT yet folded into the ClearSight Score or batch/CI `auditImageAccessibility()` (candidate follow-up: sixth score axis + batch column + verdict command-center tile). README + panel copy documented; SW cache bumped to v46.
- [x] Add a **five-axis screenshot repair safety net**: every targeted text, bulk text, component-surface, and complete accessibility repair now captures the exact pre-repair `ImageData` plus filename, original dimensions, downscale state, and provenance before changing pixels. A new **Undo last screenshot repair** action restores that snapshot, clears stale proof state, reruns all 12 simulations and five audit axes, and confirms the original audit is back—so a caught regression or unwanted visual change is reversible without re-uploading. The snapshot is single-level and resets on workspace/source changes to keep memory bounded. Browser smoke coverage drives a real component repair, verifies the improvement, undoes it, and proves the original 1.25:1 failure, source filename, and disabled undo state are restored. README updated; production bundle regenerated; offline cache bumped to v45.
- [x] Add a **developer CSS fix sheet handoff** that closes the last gap between pixel proof and shipped code: new pure `buildCssFixSheet()` + `CSS_FIX_SHEET_DEFAULTS` in vision-core turns every failing text region (WCAG 1.4.3, paired with the same `suggestAccessiblePairs` replacement the repair buttons use) and every failing component surface (WCAG 1.4.11, using the exact axis-aware `planComponentSurfaceRepair` plan via a new shared `planComponentRepairForFinding()` helper — refactored out of both repair paths so repairs and exports can never diverge) into one deterministic, paste-ready stylesheet: `:root` custom properties (`--clearsight-text-N-color/-surface`, `--clearsight-component-N-surface/-text`) with `/* was #HEX */` provenance, plus per-fix selector rules whose comments state measured before→after ratios, targets, APCA Lc for recolored labels, palette-safety verdicts, and explicit best-effort labels when a target is unreachable. In-app: **Copy fixes as CSS** (clipboard with download fallback) + **Download fix sheet (.css)** buttons under the complete-repair action, enabled only while repairable failures exist and updating live as repairs land; sheet bundled into the submission package zip as `<name>-fixes.css` with a manifest row; `buildCssFixSheetText` test hook. Also fixed the stale four-axis score placeholder copy to name all five axes. 5 new unit tests (139 total) incl. a real detect→scan→plan→sheet round trip; 6 new smoke checks (123 total) incl. driving the real download button and proving the sheet drops the surface fix after the live repair while keeping the remaining text fix. README updated; SW cache bumped to v44.
- [x] Add a unified one-click complete accessibility repair that fixes all detected text and WCAG 1.4.11 component-surface failures in one local pass, reruns all 12 simulations and five audit axes, and shows measured before→after finding and pixel-change proof.
- [x] Turn shareable audit links into a private remediation tracker: recipients can check off prioritized actions, see live completion progress, persist status locally per exact audit link, and copy a handoff-ready markdown action plan without any server or account.
- [x] Add zero-upload portfolio PDF export directly to the browser batch-audit workflow, including the fleet score, release gate, cross-screen debt map, baseline deltas, ranked screen findings, skipped-file transparency, unit coverage, and end-to-end smoke verification.
- [x] Add simulation impact ranking to make high-risk vision modes immediately visible after render.
- [x] Add source-vs-simulated slider comparison inside each simulation card.
- [x] Add unit/integration coverage for impact metric calculations.
- [x] Add drag-and-drop image loading and clipboard-paste upload flow for faster demo iterations.
- [x] Add contact-sheet export to download source + completed simulation previews as one PNG for submission packaging.
- [x] Add ranked simulation card ordering so highest-impact previews appear first after each render.
- [x] Make contact-sheet export rank-aware with visible impact delta metadata for faster judging and review.
- [x] Add one-click accessibility audit report export (JSON) covering ranked simulation impact and current contrast checks.
- [x] Add global simulation blend control to synchronize source-vs-sim overlay across all cards for faster review.
- [x] Add workspace reset control to clear source, simulations, and contrast state between demo iterations.
- [x] Add keyboard-accessible modal behavior (focus restore, tab trap) and one-key shortcuts for demo workflow.
- [x] Add single-click submission package export that downloads rendered visuals, ranked contact sheet, and accessibility report together.
- [x] Add an accessibility health summary panel combining top-impact simulation delta and contrast status.
- [x] Add global simulation intensity slider to tune matrix/filter severity across all preview modes.
- [x] Add live auto-re-render for severity slider changes with debounced updates (no manual re-run needed).
- [x] Add one-click top-impact export pack for source + highest-impact simulations with a compact contact sheet.
- [x] Add inspectable full-size simulation preview modal with direct download for focused judging review.
- [x] Add source-simulation blend control inside modal preview for high-resolution focused inspection.
- [x] Make WCAG contrast checker context-aware with large-text AA threshold support (3:1) and align suggestions/reporting output.
- [x] Add source-image color picker controls for direct contrast text/background sampling from the rendered source.
- [x] Add quick-access top-impact simulations filter toggle for rapid high-risk review during demos.
- [x] Add one-click highest-impact preview action (button + keyboard shortcut) for immediate demo triage.
- [x] Add one-click contrast suggestion raw pair-copy action with clipboard fallback for blocked environments.
- [x] Add an in-app judge-focused onboarding workflow panel and mirrored README execution checklist for faster demo clarity.
- [x] Add discoverable keyboard shortcut help panel with `?`/`H` toggle and full shortcut legend.
- [x] Expose the existing top-impact preview action and keyboard-shortcut help directly in the simulator UI (details panel + top-impact inspect button).
- [x] Persist demo settings across reloads (global comparison, simulation intensity, contrast defaults, large-text mode) for frictionless repeat demos.
- [x] Fix runtime blockers from a syntax regression by removing an extra closing brace in `docs/app.js` and `docs/current/app.js` so the app parses and runs reliably.
- [x] Add a judge-focused onboarding clarity layer in the production and current UI with an explicit 90-second contract, artifact milestones, and required handoff outputs.
- [x] Add a real-time simulation render progress rail with per-mode completion feedback.
- [x] Add frame-friendly simulation rendering by yielding to `requestAnimationFrame` between mode passes so progress and controls remain responsive on larger images.
- [x] Add judge summary markdown export with ranked simulations, contrast state, and suggestion payload for fast reviewer handoff.
- [x] Include markdown judge summary in the one-click submission package export so downloaded bundles contain both machine-readable and human-readable reporting.
- [x] Add one-click judge summary copy action in the demo panel for fast clipboard handoff during live judging.
- [x] Add an in-app judge snapshot panel that surfaces top-impact simulation risk, contrast status, and one-click copy for quick judge handoff.
- [x] Add one-click accessibility-report JSON copy action (`X` shortcut) for fast clipboard handoff and reviewer sharing.
- [x] Add one-click accessibility-report CSV copy action (`Y` shortcut) for spreadsheet-ready handoff.
- [x] Add one-click quick judge workflow action (`Q`): load sample, render all simulations, run contrast check + suggestions, and open top-impact preview for immediate demo triage.

- [x] Re-sync the `docs/` production bundle from `docs/current/` to ship the latest judge snapshot workflow and copy tools.
- [x] Add accessibility report CSV export (mode-level pixel impact and contrast status) for quick spreadsheet review in judge and submission handoff flows.
- [x] Add submission package manifest export (`submission-manifest.txt`) that lists package assets and ranked simulation metadata for judge sanity checks.
- [x] Add one-click submission-manifest clipboard copy action and `M` shortcut for manifest handoff during live demos.
- [x] Add one-click submission manifest download action so judges can save `submission-manifest.txt` directly as a file.
- [x] Add one-click contrast-result copy action with a keyboard shortcut for fast accessibility-check handoff during demos.
- [x] Add one-click contrast auto-fix action (`F` shortcut) that applies the strongest accessible pair when AA fails and refreshes contrast suggestions.
- [x] Standardize image import validation and error messages across input, drag/drop, and paste paths with explicit supported format feedback.
- [x] Add single-click self-contained reviewer packet export (HTML bundle) for judge handoff with embedded source/sim previews, ranking, and suggestion summary.
- [x] Add `prefers-reduced-motion` styling to avoid visual motion/transitions for users who request reduced motion.
- [x] Add upload-time automatic downscaling for oversized images so local rendering remains stable and fast on high-resolution screenshots.
- [x] Expand contrast input parsing to accept `hsl()`/`hsla()` values for checker text/background fields.
- [x] Add a dedicated quick-judge workflow panel with onboarding steps to clarify the recommended 60-second demo path.
- [x] Add an interactive workflow progress checklist that updates as source image loads, simulations complete, and contrast checks run.
- [x] Fix `Q` keyboard shortcut regression by calling `runQuickJudgeWorkflow()` from the shortcut map so the quick demo flow runs correctly.
- [x] Add cancellable simulation rendering (process button turns into cancel state and Escape interrupts active rendering) for safer long-running operations.
- [x] Add a visible accessibility risk badge and export-linked risk metadata (JSON, CSV, judge summary, reviewer packet, manifest) to make one-glance triage clearer for judges.
- [x] Expand contrast checker acceptance to 4- and 8-digit hex notation by stripping alpha channels before contrast math.
- [x] Add one-click handoff packet copy action that packages judge summary, manifest, accessibility report JSON, and CSV into one clipboard artifact.
- [x] Add one-click full handoff bundle copy action (`ClearSight handoff bundle`) that includes the handoff packet, accessibility report JSON/CSV, manifest, and machine JSON payload in a single markdown artifact for faster judge sharing.
- [x] Add one-click handoff packet JSON copy action with machine-readable output (compact schema + manifest text) and `N` shortcut.
- [x] Add source-image provenance metadata (original dimensions + downscale state) into all export/report artifacts (JSON, CSV, manifest, judge summary, reviewer packet) for full transparency.
- [x] Add prioritized remediation action plan into judge summary/report outputs (quick snapshot, markdown, reviewer packet, and manifest) so handoff artifacts include immediate next steps.
- [x] Add copy fallback flow for blocked clipboard access: export all copy-to-clipboard handoff/report actions as downloadable text artifacts when clipboard copy is not available.
- [x] Add contrast pair CSS snippet copy actions (current pair and suggestion cards) for faster design-system handoff.
- [x] Add filterable shortcut-help panel with no-match feedback for faster keyboard workflow discovery during demos.
- [x] Add checklist screenshot export flow (source + required simulation outputs + contrast snapshots) with canonical Devpost filename targets for consistent submission packets.
- [x] Extend contrast color parsing to accept `rgb()`/`rgba()` input formats in the checker and update parser tests/implementation in both docs and docs/current bundles.
- [x] Align checker contrast inputs with the parser so `rgb()`/`rgba()`/`hsl()`/`hsla()` entries are accepted in the UI text fields with updated validation messaging.
- [x] Expand contrast parser to accept CSS color names (e.g. `rebeccapurple`, `dodgerblue`) and relax manual color-input length limits while updating validation messaging for in-UI guidance.
- [x] Harden contrast color parsing to reject non-standard `rgb()`/`hsl()` alpha syntax while preserving valid `rgba()`/`hsla()` and modern slash forms.
- [x] Make impact summary chips interactive, opening ranked simulation previews directly from the summary row for faster high-impact inspection.
- [x] Prevent shortcut actions from firing during browser/system modifier-key combos (Ctrl/Cmd/Alt + key), so standard shortcuts remain usable during demos.
- [x] Auto-generate a one-click contrast remediation action in the checker output so failed AA checks show the best accessible pair and apply it immediately.
- [x] Add HTTP(S) image URL loading directly in the Source section to load external screenshots quickly with validation and clear load/error handling.
- [x] Add one-click contrast snapshot PNG export from the checker for quick visual handoff and screenshot-ready contrast evidence.
- [x] Add one-click contrast suggestion list CSV export (plus clipboard fallback download) so full batch recommendations can be shared quickly in judge handoff.
- [x] Add contrast suggestion JSON copy export (plus fallback download) for machine-readable palette handoff.
- [x] Add estimated raw image payload sizing metadata to submission manifest entries (per-file and total) so judges can quickly sanity-check package export scale.
- [x] Improve `suggestAccessiblePairs()` so generated color suggestions stay visually close to source colors (adaptive, source-aware candidate generation) while still meeting target contrast.
- [x] Add fallback-aware suggestion mode so contrast suggestions still surface closest alternatives when exact WCAG target is unattainable, with UI fallback messaging.
- [x] Add preview modal carousel controls and keyboard navigation (Prev/Next, index label, Home/End, arrow keys) for quick review of rendered simulations.
- [x] Add production-facing UI polish and accessibility onboarding upgrades: skip-link, stronger focus treatment, and refined card/hover states for a higher-fidelity demo presentation.
- [x] Improve end-to-end onboarding clarity with a synchronized 90-second judge workflow and explicit artifact expectations in `docs/current/index.html`, `docs/index.html`, and `README.md`.
- [x] Improve HTTP(S) image URL import resilience by validating decodable image blobs (including missing/misleading content types), adding URL fetch timeout handling, and clearer non-image/CORS failure messages.
- [x] Add clipboard plain-text image URL import so pasted image links auto-load with existing URL validation and CORS handling.
- [x] Improve clipboard image URL parsing by stripping wrappers/punctuation and scanning pasted text for valid URLs before fallback validation.
- [x] Add live, debounced contrast checker re-computation when checker color inputs change so results update automatically as valid values are entered.
- [x] Add judge submission-readiness indicator in the quick workflow checklist and help text so users can tell exactly when judge-ready exports are available.
- [x] Clarify the 90-second judge workflow and in-app onboarding copy for faster first-pass success.
- [x] Add one-click contrast text/background swap control (`Swap text/background`) and `W` shortcut for faster WCAG contrast iteration.
- [x] Add accessibility handoff packet markdown + JSON files to the one-click submission package export so judges get a complete reviewer bundle from a single download.
- [x] Add contrast target margin reporting (AA/AAA/large-text delta in-terms-of ratio difference) across contrast check output, judge snapshots, accessibility report, CSV export, and submission manifest.
- [x] Make completed simulation cards directly keyboard-and-pointer activatable so Enter/Space and click open the preview modal and keep card-specific inspect flow accessible.
- [x] Extend contrast suggestion generation so a pair that already passes AA but fails AAA automatically suggests premium AAA-upgrade replacements when requested, and surface this status in the accessibility summary.
- [x] Add a live judge-readiness score meter in the workflow panel to show completion progress and next action for judge submission readiness.
- [x] Gate judge-packaging and handoff actions (summary, manifest, handoff, package, snapshots) until source, simulations, and contrast check are complete.
- [x] Refine readiness scoring and workflow gating to require all simulations be fully rendered (`.is-done`) before judge-ready unlock, preventing incomplete/aborted render sessions from exposing handoff success states.
- [x] Tighten onboarding copy by adding explicit phase-to-artifact unlock messaging in `docs/index.html`, `docs/current/index.html`, and `README.md` so first-time judges can predict exactly when exports are enabled.
- [x] Refresh onboarding microcopy with a phase-to-action lock map so disabled artifact controls clearly signal readiness dependencies during live demos.
- [x] Add a live 90-second judge workflow timer with phase cues and controls, and auto-start on quick demo flow.
- [x] Add an interactive preview modal slideshow with play/pause controls for rapid simulation review during demo walkthroughs.
- [x] Add keyboard navigation across simulation cards using arrow keys plus Home/End so users can review simulations in a full keyboard-first flow.
- [x] Include canonical checklist screenshot assets (source, simulations, and contrast snapshots) in the one-click submission package export so judges can pull a complete packet in one action.
- [x] Add a keyboard shortcut for one-touch submission package download (`Z`) and document it in shortcut help and onboarding copy.
- [x] Add an "Undo last contrast change" button (`O` shortcut) and state tracking so contrast auto-fix/suggestion applications can be rolled back safely during demo iterations.
- [x] Auto-run contrast checks after successful simulation rendering so checklist and readiness states prime immediately for faster demos and fewer manual clicks.
- [x] Clarify contrast checker color-format guidance in live UI controls and generated demo script text to match supported parser formats (hex, rgb/rgba, hsl/hsla, CSS names).
- [x] Add a one-click judge handoff action that downloads the submission package and copies the handoff packet (`G`) once source/simulations/contrast are complete.
- [x] Improve contrast auto-fix behavior to use fallback suggestion generation when exact AA matches are unavailable, and display clearer fallback-quality status.
- [x] Add an `npm run build` sync script so `docs/` is regenerated deterministically from `docs/current/` before release.
- [x] Add contrast suggestion quality metadata (AA-ready / AAA-ready / Below AA) in checker UI badges plus JSON/CSV/judge summary/reviewer packet exports.
- [x] Convert the submission package export into a true single-archive download (`submission-package.zip`) containing all visual artifacts plus report/manifests for one-click judge handoff.
- [x] Add phase-aware quick-start timeline progression (Load → Render → Contrast → Export) that updates live from checklist/ timer state in the onboarding panel.
- [x] Harden submission package image export by exporting canvas PNGs via async `toBlob` binary conversion with a safe `toDataURL` fallback for edge-case compatibility.
- [x] Add polished staging reveal and section-specific card hierarchy in the production UI (`docs/current` → `docs`) to improve demo-first visual clarity and professionalism.
- [x] Add a judge playbook microcopy pass in onboarding and README so judges can recover quickly when a phase falls behind schedule.
- [x] Add submission-readiness and expected package-artifact metadata to the judge snapshot copy output for clearer handoff communication.
- [x] Add direct contrast suggestion list downloads (CSV + JSON) and include the generated suggestion artifacts directly in the submission package zip/manifest.
- [x] Add judge workflow snapshot copy artifact (`E` shortcut) for a single shared-readiness + required artifact checklist handoff summary during judging.

- [x] Expand URL/image import compatibility to include SVG and validate URL responses with image decoding when MIME metadata is missing or ambiguous.

- [x] Improve manual preview and checklist image exports to use `canvas.toBlob()` with a safe `toDataURL()` fallback for better reliability and lower memory pressure on large captures.
- [x] Add per-simulation visual change hotspot maps that preserve source context and highlight the exact UI regions most altered by each vision mode.
- [x] Fix low-vision severity rendering so blended filter output is painted back to preview canvases and matches its reported impact score.
- [x] Replace the above-the-fold onboarding wall with a compact judge mission-control experience: one primary guided-demo action, four-phase timeline, live readiness, timer, and collapsible artifact guidance.
- [x] Add end-to-end browser smoke test (`npm run smoke`, `scripts/smoke.mjs`): serves `docs/` over HTTP, drives the full quick-judge workflow in headless Chrome (sample → 9 simulations → contrast → suggestions → preview modal → readiness 100%), and fails on any console/page error. Requires `npm install` (puppeteer-core) plus a cached/system Chrome.
- [x] Fix critical contrast-checker crash found by the smoke test: `resolveContrastSuggestionExportPayloads`/`buildContrastSuggestionsCsv`/`buildContrastSuggestionsJson` called `renderContrastResult()` from inside the judge-snapshot chain that `renderContrastResult` itself triggers, causing infinite recursion → stack overflow → "Cannot read properties of null (reading 'aaaThreshold')" on every contrast check once simulations were rendered (readiness stuck at 75%, all judge exports locked). Fixed with an `isRenderingContrastResult` re-entrancy guard; suggestion JSON now also reads full threshold/margin fields from `state.lastContrastResult` instead of the bare `evaluateContrast` result (which never had them).
- [x] Make ClearSight installable and genuinely offline-ready with a subpath-safe web app manifest, app icon, service-worker app-shell cache, resilient navigation fallback, install prompt, and live offline proof in the hero.
- [x] Add one-click "accessible recolor" preview: `buildAccessibleRecolorPlan()` greedily picks minimal-shift replacement colors that fix below-AA dominant-palette pairings without breaking pairs that already pass, `applyPaletteRemapToImageData()` repaints the actual screenshot via nearest-anchor delta-preserving pixel remap, and the palette audit panel shows original vs repainted canvases, substitution chips, honest fixed/unfixable counts, and a PNG download. Covered by 6 new unit tests plus smoke assertions (recolor checks run after the preview modal closes — the modal overlay otherwise swallows the click). Service-worker cache bumped to `clearsight-static-v2`.
- [x] Add automatic text contrast scanning: `detectTextLikeRegions()` in vision-core (edge-density tiling + connected components + 2-means luminance clustering) finds text-like regions in the screenshot, estimates each region's fg/bg colors and WCAG ratio, and classifies AA/AAA/large-text levels. New "Automatic text contrast scan" panel outlines regions on the source (color-coded, numbered, worst-contrast first) and loads any region's pair into the checker with one click — no manual color picking. Covered by 3 new unit tests (49 total) plus smoke assertions (region detection, worst-first ordering, overlay painting, checker handoff); the demo sample's failing Export button (3.86:1) is caught automatically. Service-worker cache bumped to `clearsight-static-v4`. (Follow-up shipped: scan results are now included in all report/manifest exports — see entry below.)
- [x] Make automatic text findings judge-ready with a one-click annotated PNG export containing the original screenshot, numbered callouts, contrast ratios, colors, and WCAG classifications; include the artifact automatically in the submission ZIP and verify export readiness in the browser smoke test. Service-worker cache bumped to `clearsight-static-v5`.
- [x] Ship the text-scan → exports follow-up: automatic text contrast scan findings now flow into every judge-facing artifact — accessibility report JSON (`textScan` section with ranked regions, colors, ratios, WCAG levels), report CSV (`text_scan_regions`/`text_scan_below_aa`/`text_scan_worst_ratio`/`text_scan_worst_pair` columns), judge summary markdown (snapshot line + worst-first region table), reviewer packet HTML (summary line + findings table), submission manifest (scan block), and the prioritized remediation plan (high-priority action naming the worst below-AA region's colors, ratio, and position). Added read-only `window.__clearsightReportHooks` so the smoke test asserts artifact contents directly; 6 new smoke checks verify the demo sample's 3.86:1 Export-button finding reaches JSON/CSV/markdown/HTML/manifest/remediation. Service-worker cache bumped to `clearsight-static-v6`.
- [x] Close the remediation loop with one-click "Use fix & verify again": promote the generated accessible recolor to the active source, automatically rerun all nine vision simulations and palette audit, then show a persistent before→after proof card with below-AA pair reduction and peak simulation-shift comparison. Extended the browser smoke test to exercise the full detect → fix → re-audit workflow and bumped the offline app shell to `clearsight-static-v3` so returning users receive it.
- [x] Turn the automatic text scan into an interactive issue navigator: click a numbered screenshot region (or use “Review worst finding”) to load its sampled colors, run WCAG analysis, generate accessible fixes, and jump directly to remediation controls; preserve keyboard access with Enter/Space on the scan canvas.
- [x] Add one-click "Export vision reel (video)": records a WebM entirely client-side via `canvas.captureStream()` + `MediaRecorder`, cycling the original view and all nine simulations (highest impact first) with mode labels, pixel-shift percentages, frame counters, and an animated progress strip baked into each frame. Recorded reels are cached in state, auto-bundled into the submission package ZIP, and listed in the manifest (`kind: Vision reel video`); stale reels are cleared on re-render/workspace reset. New pure helper `orderVisionReelSegments()` in vision-core (source first, sims by impact desc, unmeasured last) with 3 unit tests (52 total). Smoke test records a real reel through `__clearsightReportHooks.exportVisionReel({segmentMs: 200, download: false})` and asserts 10 views + nonzero WebM bytes + manifest listing; the smoke offline check no longer hardcodes the SW cache version (matches `clearsight-static-*`). VIDEO_SCRIPT.md and README document splicing the reel into the demo video. Service-worker cache bumped to `clearsight-static-v8`.
- [x] Add an automatic text-scan “issue lens”: selecting a numbered finding now opens a magnified contextual source crop with a clear WCAG verdict, sampled foreground/background chips, and an attached “Fix this contrast” action. The worst finding is focused automatically after every scan, making small UI failures legible in live demos; browser smoke coverage verifies the lens is visible and painted. Service-worker cache bumped to `clearsight-static-v9`.
- [x] Expand the simulator from 9 to 12 vision modes with three medically-grounded low-vision conditions: Cataracts (haze + glare via canvas filter), Glaucoma (tunnel vision / peripheral field loss), and Macular degeneration (central scotoma). Field loss is computed by a new pure `applyFieldLossMask()` in vision-core (elliptical smoothstep occlusion, validated inputs, occluded-pixel summary) driven by a new `fieldloss` mode kind in `renderMode`; both new masked modes respect the global severity slider, impact ranking, hotspot heatmaps, reels, and all exports automatically. Because occlusion modes are recolor-invariant and now top the impact ranking, the recolor fix-proof card was retargeted to compare "Peak color-vision shift" across matrix modes only (was misleadingly showing an unchanged peak). Updated all nine-mode copy (index.html, README, VIDEO_SCRIPT, demo script lines), smoke expectation to 12 cards / 13 reel views, added 3 unit tests (55 total), and bumped the SW cache to `clearsight-static-v10`. Full smoke suite passes with zero console errors.
- [x] Add CVD-projected contrast analysis that exposes "hidden contrast failures" — pairs that pass standard WCAG math for typical vision but collapse for color-blind users. New pure `projectContrastAcrossCvdModes()` in vision-core projects a text/background pair through all seven full-severity CVD matrices (the exact matrices the simulator renders with), recomputes each WCAG ratio, and reports worst mode + failing modes + a `hiddenFailure` flag (basePassesAA && any projection below AA). Integrated everywhere: contrast checker output gains a "Color-vision projected contrast" strip (7 per-mode pass/fail chips + a headline that calls out hidden failures, `#cvdProjection[data-hidden-failure]`); automatic text scan regions each carry `CVD worst N:1 (mode)` detail plus an orange `CVD risk` badge and hidden-failure counts in the scan status; accessibility report JSON embeds `contrast.lastChecked.cvdProjection` and per-region `cvdWorstRatio/cvdWorstMode/cvdHiddenFailure`; judge summary markdown adds a Color-vision projection snapshot line and a "CVD worst" column in the scan table; remediation plan adds high/medium-priority hidden-failure actions; copy-contrast text includes the worst projection. Demo sample now surfaces a real finding (worst region 3.86:1 drops to 2.75:1 under tritanopia). 3 new unit tests (60 total; red-on-black hidden failure via achromatopsia at 1.74:1, gray invariance, validation) plus 5 new smoke checks (report/summary/scan integration and a live red-on-black hidden-failure UI assertion). SW cache bumped to `clearsight-static-v12`.
- [x] Close the automatic text scanner's region-level remediation loop with one-click `Repair region & verify`: choose the closest AA-ready pair, conservatively isolate foreground/background pixel clusters inside only the selected finding, repaint that local region, and rerun all 12 simulations plus the text scan. A persistent proof card reports the measured before→after ratio and changed-pixel count (demo sample: 3.86:1 → 4.64:1, AA verified). Added pure `applyTextRegionContrastFix()` coverage (57 unit tests total), end-to-end smoke coverage, README onboarding, and bumped the offline cache to `clearsight-static-v11`.
- [x] Make contrast remediation CVD-aware: evaluate a wider candidate pool across all seven color-vision matrices, promote the closest pair that holds AA in every mode, and label every suggested pair with a visible `CVD-safe` or projected-risk badge. Added pure ranking/validation and browser workflow coverage so a typical-vision WCAG pass can no longer be presented as the best fix when it creates a hidden color-vision failure. Service-worker cache bumped to `clearsight-static-v13`.
- [x] Add APCA perceptual contrast analysis (WCAG 3 draft) as a third independent lens alongside WCAG 2 math and CVD projection — new pure `apcaContrast()`/`evaluateApcaContrast()`/`compareWcagVsApca()` in vision-core (apca-w3 v0.0.98G-4g constants, verified against six published reference values to <0.1 Lc; models 2.4-gamma screen luminance, soft black clamp, and reading polarity). The checker gains an APCA readout with signed Lc score, polarity note, four usage-tier chips (body 75+/fluent 60+/spot 30+/non-text 15+), and a **perceptual false pass** callout (`#apcaReadout[data-false-pass]`) for pairs WCAG 2 passes but APCA scores below Lc 60 — e.g. #AAAAAA on #000000: 9.04:1 (AAA pass), CVD-invariant, yet Lc 56. Text-scan regions carry `APCA Lc N` detail + `APCA risk` badges + a false-pass count in scan status; report JSON (`contrast.lastChecked.apca`, per-region `apcaLc/apcaRating/apcaFalsePass`), CSV (`apca_lc/apca_rating/apca_false_pass` columns), judge summary snapshot line, copy-contrast text, and remediation plan (high-priority false-pass action + medium region action) all embed APCA. 4 new unit tests (67 total), 7 new smoke checks incl. a live gray-on-black assertion proving the catch is APCA-only (WCAG 2 passes, 0 CVD chips fail). SW cache bumped to `clearsight-static-v14`.
- [x] Add a CVD color-collision scan (WCAG 1.4.1 Use of Color) — a fourth analysis axis that catches what no contrast lens can: color pairs that ENCODE DIFFERENT MEANINGS (status dots, chart lines, buttons) yet collapse into near-identical shades for color-blind users, even though they never touch as text/background. New pure `labDeltaE()` (sRGB→XYZ→CIE Lab, ΔE76) and `findCvdColorCollisions()` in vision-core project every clearly-distinct dominant-color pair (base ΔE ≥ 25) through all seven full-severity CVD matrices and flag pairs whose projected ΔE drops below 12 (near-identical at a glance), reporting worst mode, retention %, projected hexes, and all colliding modes (worst-first). Palette audit gains a "Color-only distinction risk (WCAG 1.4.1)" panel with side-by-side original → projected swatch duos ("Typical vision → Seen with Achromatopsia"), ΔE collapse readout, and 1.4.1 risk badges; demo UI sample surfaces 3 real collisions (teal #0F766E vs blue #0284C7 action buttons both become ~#606060 gray, ΔE 46.8 → 5.3). Findings embed in report JSON (`paletteCollisions`), CSV (3 new palette_collision_* columns), judge summary snapshot line, reviewer packet summary, and a high-priority remediation action citing WCAG 1.4.1 with the second-cue fix (icon/label/pattern). Thresholds calibrated against the app's own matrices: green/amber status colors collide (ΔE 91.7 → 2.0), amber/red collides under tritanopia, while black/white and lightness-separated pairs never flag. 5 new unit tests (72 total), 7 new smoke checks (live UI collision assertion + all export artifacts). SW cache bumped to `clearsight-static-v16`.
- [x] Add a live audit verdict command center directly beneath mission control: condense automatic text failures, hidden CVD failures, APCA perceptual risks, and peak simulation shift into four glanceable metrics, then surface one context-sensitive next action (run audit, inspect/repair the worst text region, inspect a hidden risk, or open the peak simulation). Added end-to-end smoke coverage so the guided demo must expose the real below-AA finding and route judges straight to repair. Service-worker cache bumped to `clearsight-static-v15`.
- [x] Complete the live audit verdict with WCAG 1.4.1 color-collision triage: add collision counts to the above-the-fold metrics and total risk verdict, include palette-pair coverage in the audit summary, and route collision-only audits directly to the worst collapsing pair with keyboard focus. Extended browser smoke coverage so the demo verdict cannot silently omit color-only findings. Service-worker cache bumped to `clearsight-static-v17`.
- [x] Add the **ClearSight Score** — a single deterministic 0–100 graded verdict (A–F) condensing all four analysis axes into one Lighthouse-style headline number. New pure `computeAccessibilityScore()` + `CLEARSIGHT_SCORE_GRADES` in vision-core: text contrast (weight 40, mean/worst 50-50 blend with failing regions capped at 60 pts so one hard AA failure can't be averaged away), color-vision safety (25, hidden-CVD-failure fraction), APCA perceptual contrast (15, false-pass fraction), color independence WCAG 1.4.1 (20, collision fraction of distinct pairs); axes without data are excluded and weights renormalized so partial audits score honestly. Verdict command center gains an animated conic-gradient score ring with grade-colored fill (`#clearsightScoreCard[data-score][data-grade]`), Grade X · label verdict, and per-axis breakdown line; score re-renders on every audit update so the repair/recolor loops show live grade movement (demo UI sample: 76/100 Grade C). Embedded in report JSON (`accessibilityScore` with weighted axes + details), CSV (`clearsight_score`/`clearsight_grade` columns), judge summary snapshot line, and reviewer packet summary. 5 new unit tests (77 total) covering blend math, axis penalties, weight renormalization, insufficient-data, and validation; 6 new smoke checks (live ring, real-findings bound 0<score<100, JSON/CSV/summary/packet embed, post-fix recompute). README documents the scoring model. SW cache bumped to `clearsight-static-v18`.
- [x] Add **Batch audit — score every screen at once** (multi-screenshot whole-app auditing). New pure `auditImageAccessibility()` in vision-core composes the complete four-axis pipeline headlessly (text-region detection + per-region CVD projection and APCA comparison + dominant-palette collision scan + ClearSight Score) so any RGBA buffer can be scored without the DOM, plus pure `buildBatchAuditCsv()` for one-row-per-screen spreadsheet export with proper quoting. New "1b) Batch audit" panel accepts up to 12 screenshots (multi-file picker, validation + skip reporting, 1600px audit downscale), audits each with live progress, and renders results ranked riskiest-first: rank, thumbnail, per-screen finding counts (below-AA / hidden CVD / APCA risk / color-only collisions), grade-colored mini score ring, and an `Open full audit` button that promotes the screen into the main 12-simulation workspace (batch results survive that navigation; only explicit `Clear workspace` discards them). `Score built-in sample set` demos instantly with the two demo scenes — dashboard 64/100 Grade D vs UI 78/100 Grade C — and `Download batch CSV` exports the ranked table. Smoke hooks added (`runBatchSampleAudit`/`getBatchAudit`/`buildBatchAuditCsvText`). 3 new unit tests (80 total), 5 new smoke checks (rendering, ranking, summary, axis counts, CSV). README documents the workflow; SW cache bumped to `clearsight-static-v20`.
- [x] Add persistent score outcome proof to both automated remediation loops: capture the pre-fix ClearSight Score before targeted text repair or full accessible recolor, then show a verified score and grade before→after delta beside the live score ring after the re-audit. Broad fixes that improve one metric but lower the overall score are explicitly labeled “Regression caught” with a do-not-adopt warning (the demo recolor catches a real 76→72 regression), instead of being falsely celebrated. Reset clears the proof, reduced-motion users skip its arrival animation, and the offline cache is bumped to `clearsight-static-v19`.
- [x] Turn multi-screen batch audit into an executive accessibility debt map: compute a weakest-screen-aware portfolio score (75% fleet average + 25% lowest route), grade distribution, cross-screen finding totals, weakest system axis, and an explicit release-ready / needs-review / blocked gate. Added pure `summarizeBatchAudit()` coverage, exposed the aggregate through smoke hooks, documented the product-level workflow, and bumped the offline cache to `clearsight-static-v21`.

- [x] Ship **ClearSight CI** — a zero-dependency command-line screenshot auditor (`npm run audit -- screens/*.png --min-grade B`) that runs the exact production four-axis pipeline headlessly in pure Node: new `scripts/lib/png.mjs` implements a from-scratch PNG decoder (chunk parsing + `node:zlib` inflate + all five scanline filters; 8-bit gray/RGB/RGBA/gray-alpha and 1/2/4/8-bit palette PNGs with tRNS; loud, actionable errors for 16-bit/interlaced/non-PNG input) plus an O(n) box-average `downscaleRgba()` matching the in-app 1600px working resolution, and `scripts/audit-cli.mjs` decodes each screenshot, runs `auditImageAccessibility()` from `docs/js/vision-core.js` (byte-identical math to the web app), ranks riskiest-first, prints a finding-count table + `summarizeBatchAudit()` portfolio debt map, writes optional `--csv`/`--json` artifacts, and gates CI via `--min-score`/`--min-grade` with CI-friendly exit codes (0 pass/report-only, 1 gate failed, 2 usage/decode error; unscorable screens always fail a gate). 8 new unit tests (90 total) include an in-test PNG *encoder* (CRC32 + deflate + forward filters) for real byte-level round trips and an end-to-end PNG→audit→rank→table run. README gains an "Audit screenshots in CI" section with a GitHub Actions example; the in-app batch panel now advertises the CI command (SW cache bumped to v22). Works directly on Playwright/Cypress/Puppeteer screenshot output — no browser, no npm dependencies.
- [x] Add **accessibility regression gating** to ClearSight CI: `--baseline previous-audit.json` matches screens by filename and fails only when a current ClearSight Score drops, while `--max-score-drop N` supports intentional tolerance. New screens remain visible without being falsely compared, formerly scored screens becoming unscorable fail safely, absolute `--min-score`/`--min-grade` gates compose with the regression gate, and JSON output embeds the full per-screen comparison for PR automation. Added focused parser/comparison tests, README + in-app CI guidance, and bumped the offline cache to v23.

- [x] Add **Capture a live app** — one-click screen-capture import that audits any running application with zero screenshot files. New `captureLiveAppFrame()` in app.js uses `getDisplayMedia` (window / browser-tab / full-screen picker, `selfBrowserSurface: 'exclude'`), waits for a real presented frame via `requestVideoFrameCallback` (timeout fallback), grabs exactly one frame to a canvas, stops all tracks immediately (privacy: sharing ends the instant the frame is captured, nothing leaves the device), encodes PNG, and feeds the existing `handleImageInput()` pipeline so the frame gets the complete four-axis audit + 12 simulations automatically. Graceful paths: unsupported browsers get a disabled button with explanatory title + error message, user-cancelled pickers show a friendly info message (NotAllowedError/AbortError), NotFoundError and zero-dimension frames get specific errors, and the button participates in render-time control gating (`setImageControlsEnabled`). New smoke hooks `captureLiveAppFrame`/`getSourceMeta`; 3 new smoke checks drive the real button through a stubbed `getDisplayMedia` backed by an animated `canvas.captureStream()` and assert the frame loads as source, renders all 12 simulations at true capture size (640×360), and re-enables the button. README + Source-panel hint documents the flow; SW cache bumped to v24.
- [x] Add **GitHub-facing CI artifacts** to ClearSight CI: `--markdown pr-comment.md` writes a PR-comment-ready GitHub-flavored-markdown report (portfolio score + grade headline, release-gate emoji verdict, weakest axis, ranked riskiest-first table with per-screen axis findings and baseline Δ-score column with 🔺/🔻/🆕 markers, score + regression gate verdicts, collapsible skipped-files section, pipe-escaped filenames) ready for `gh pr comment --body-file`; `--badge clearsight-badge.svg` writes a shields-flat-style SVG badge showing the portfolio score colored by grade band (A green → F red, gray `unscored` fallback) for README embedding. New pure builders in `scripts/lib/report.mjs` (`buildMarkdownReport`, `buildScoreBadgeSvg`, `gradeForScore`, `GRADE_COLORS`) with no I/O; CLI wires flags + help text. 2 new unit tests (94 total) cover table/gate/delta/skip rendering, markdown pipe escaping, report-only mode, badge grade colors, and the unscored badge. README CI section documents both flags with a full GitHub Actions gate + `gh pr comment` job; in-app batch CI hint advertises them. SW cache bumped to v26.
- [x] Add **GitHub code-scanning SARIF output** to ClearSight CI: `--sarif clearsight.sarif` emits SARIF 2.1.0 with screenshot artifact locations and distinct rules/results for score-floor failures, baseline regressions, below-AA text, hidden CVD contrast, APCA perceptual risks, and WCAG 1.4.1 color collisions. README includes an `upload-sarif` GitHub Actions step, the in-app CI hint advertises code-scanning annotations, and pure report tests cover failing and clean audits (95 total). Offline cache bumped to v27.
- [x] Add **Animation flash scan — photosensitive seizure safety (WCAG 2.3.1)**, the first motion-aware analysis surface: new pure `analyzeFlashRisk()` + `FLASH_RISK_LABELS` in vision-core approximates the general flash threshold — per-frame mean relative luminance over an 8×8 region grid, qualifying transitions (≥0.1 luminance swing with the darker state below 0.80, per the WCAG 2.3.1 definition), disjoint opposing-pair flash counting, rolling 1s windows anchored at flash times, peak flash rate + violating-area share (>3/sec per block = violating; ≥25% simultaneous area = high risk, any block at/above 3/sec = caution), worst-window report, and a luminance timeline for sparkline rendering. New "1c) Animation flash scan" panel decodes animated GIF/APNG/WebP via WebCodecs `ImageDecoder` (≤240 frames, ≤320px working size, truncation reported; graceful disabled state + title on unsupported browsers), plus `Run built-in flashing demo` which synthesizes a strobing promo banner client-side (works in every browser, no decoder needed) and flags it high-risk at 5 flashes/sec over 62.5% of the frame. Results render a grade-colored verdict, four stat tiles, a risk-colored frame-luminance timeline with the 0.80 exemption line, and risk-specific remediation guidance. Findings embed in report JSON (`flashScan`), the judge summary snapshot ("Animation flash scan (WCAG 2.3.1)" line), and the remediation plan (high-priority action for threshold failures, medium for caution); `Clear workspace` discards the scan alongside batch results. Hooks `runFlashScanDemo`/`getFlashScan`; 5 new unit tests (100 total) covering strobe=high, slow blink=low, small-area=caution, bright-only flicker exemption, and validation; 3 new smoke checks drive the real demo button end-to-end (high-risk verdict, painted timeline + stat tiles, report/judge-summary embed). SW cache bumped to v28.
- [x] Add **browser-native accessibility regression review** to Batch audit: import the JSON report emitted by ClearSight CI, match current screens by filename, and surface a pass/fail regression gate plus per-route before→after score deltas (improved, unchanged, new, or regressed) directly beside each score ring. The comparison stays fully local, validates malformed reports, works whether the baseline is loaded before or after screenshots, and connects the interactive debt map to the existing `--baseline` CI workflow. Added pure comparison coverage and an end-to-end browser assertion with one real improvement and one forced regression; SW cache bumped to v25.
- [x] Complete the WCAG 2.3.1 motion audit with **saturated-red flash detection**: each 8×8 screen region now tracks R/(R+G+B) saturation plus CIE 1976 UCS chromaticity, independently counts opposing red transitions at the official ≥0.8 saturation / >0.2 chromaticity thresholds, and combines red/general rates for the area verdict. Replaced the generic demo with a red↔green “trap” that produces zero qualifying luminance flashes but fails at 5 saturated-red flashes/sec across 62% of the frame; UI stats, guidance, report JSON, README, unit tests, and browser smoke coverage now prove the hidden failure. Offline cache bumped to v29.
- [x] Extend the flash scan to **real video files (MP4/WebM/MOV)** — the panel is now "Animation & video flash scan" and audits the content where seizure risk actually lives: product-demo videos, screen recordings, and hero-banner clips. New pure `planVideoFrameSampling()` in vision-core (uniform 10 fps seek plan, 240-frame/24 s cap with truncation reporting, ≥300 ms minimum, validated inputs; 3 new unit tests, 104 total). `decodeVideoFrames()` in app.js seek-samples a `<video>` element ≤320px into the existing `analyzeFlashRisk` pipeline: handles the MediaRecorder WebM `duration: Infinity` quirk (seek-to-end fix), races `seeked` with a no-op-seek poll so it can never hang, and waits on `requestVideoFrameCallback` after each seek because `seeked` alone can present the stale pre-seek frame. The scan file button is no longer disabled without WebCodecs `ImageDecoder` — video scanning works in every modern browser; only the animated-image path needs it (clear per-path error message). Accept list/copy/README updated; new `scanFlashFile` hook + 2 smoke checks that record a real strobing WebM in-page (MessageChannel task loop + `CanvasCaptureMediaStreamTrack.requestFrame()`, because headless clamps setTimeout/rAF to ~200 ms which aliased the strobe — smoke browser now also launches with background-timer-throttling disabled) and assert the full decode→sample→verdict pipeline flags it high-risk at 5 flashes/sec over 100% area. SW cache bumped to v30.
- [x] Turn the accessible recolor into an interactive **before/after proof slider**: the original and fixed screenshot now share a wipe comparison with a draggable divider, persistent side labels, keyboard-operable range control, and an explicit accessible percentage value. The reveal resets predictably for each new audit and has end-to-end smoke coverage. Offline cache bumped to v31.
- [x] Ship **Record app walkthrough** — zero-screenshot whole-app auditing. The batch panel can now record a live screen share while the user clicks through their product: new pure vision-core keyframe state machine (`computeFrameSignature()` — per-cell mean RGB over an 8×8 grid, `frameSignatureDistance()` — normalized 0..1 mean channel delta, `evaluateWalkthroughFrame()`/`selectWalkthroughKeyframes()` + `WALKTHROUGH_KEYFRAME_DEFAULTS`) classifies every 600 ms sample as keep / duplicate / transition / overflow, so only *stable, distinct* screens are captured — mid-navigation paints are skipped and revisiting a screen never duplicates it. In-app, `startWalkthroughRecording()` reuses the getDisplayMedia capture plumbing (self-surface excluded), samples signatures from a 96px downscale, snapshots keyframes at ≤2400px, live-updates the batch status line + a red pulsing `Stop & audit N screens` button (reduced-motion safe), auto-stops at 12 screens or 2 minutes or when sharing ends, and `Stop & audit` feeds `walkthrough-screen-NN.png` entries through the existing ranked batch audit, portfolio debt map, CSV export, and `Open full audit` promotion (via canvas→File). Clear workspace discards an active recording; cancel/permission errors are handled per path. 6 new unit tests (110 total) cover signature math, dedupe/revisit semantics, overflow, end-to-end raw-frame selection, and validation; 3 new smoke checks drive the real button with a fake display stream cycling three painted app scenes and prove 3 keyframes + skipped repeats + a full batch audit of the recording. README + panel copy document the workflow; SW cache bumped to v32.
- [x] Make **Record app walkthrough** a combined static + motion audit: while the 600 ms keyframe state machine captures distinct screens, a parallel 10 fps / 320 px sampler now preserves up to 240 live frames and runs the existing general-luminance + saturated-red WCAG 2.3.1 analyzer the moment recording stops. One screen share now produces both the ranked multi-route portfolio debt map and a seizure-safety verdict (including timeline, report JSON, judge summary, and remediation guidance); motion sampling caps honestly at 24 seconds while static screen discovery can continue for 2 minutes. Recording status exposes both captured screens and analyzed motion frames, cleanup stops both timers, browser smoke asserts that a real walkthrough creates both artifacts, onboarding/README explain the combined contract, and the offline cache is bumped to v33.

- [x] Ship **PDF audit report export** — a from-scratch, zero-dependency PDF 1.4 writer in vision-core. New pure `buildPdfReport(doc)` emits raw PDF bytes from a small declarative document model (Letter pages, Helvetica/Helvetica-Bold with WinAnsi encoding + unicode sanitization map, word wrap with per-class glyph metrics, key-value rows, truncating tables with repeated headers on page breaks, spacers, pagination with "Page N of M" footers, a grade-colored score donut drawn with bezier arcs, and an aspect-fit DCTDecode JPEG XObject with border), with validated inputs and byte-accurate xref offsets; `buildAuditPdfDoc(report, {image})` maps the existing accessibility-report JSON into that model (score + axis weights, findings overview, worst-first text-scan table with CVD/APCA columns, WCAG 1.4.1 collision table, manual contrast lenses incl. HIDDEN FAILURE / PERCEPTUAL FALSE PASS callouts, simulation impact table, flash-scan verdict, prioritized remediation plan, audited screenshot). In-app: **Download PDF audit report** button (gated with the judge exports), source thumbnail embedded as JPEG via canvas, `<name>-audit-report.pdf` automatically added to the submission-package ZIP + manifest + judge-snapshot expected outputs, and a `buildAuditPdfBytes` smoke hook. In CI: new `--pdf out.pdf` flag writes a portfolio PDF (portfolio score donut colored by grade, debt map key-values, riskiest-first table with per-screen axis findings and baseline Δ column, CI gate verdicts, skipped-file note) via pure `buildPortfolioPdfDoc()` in scripts/lib/report.mjs. 7 new unit tests (117 total) including xref-offset verification against every object header, pagination/footer numbering, JPEG embedding, WinAnsi sanitization/escaping, report mapping, portfolio doc with baseline deltas, and validation; 4 new smoke checks build a real 39KB PDF in-browser after the judge flow (valid header/EOF, score+findings+remediation text, JPEG XObject, manifest listing). Rendered output visually verified via sips rasterization (app report, CLI portfolio report, image placement). README + panel copy + CLI help documented; SW cache bumped to v34.

- [x] Ship **Shareable audit link — the whole verdict travels in the URL, zero upload**. New pure `buildShareableAuditPayload(report)` / `parseShareableAuditPayload(payload)` + `SHAREABLE_AUDIT_VERSION`/`SHAREABLE_AUDIT_LIMITS` in vision-core compact the accessibility report into a capped, validated v1 schema (score + axes, top-6 simulations, worst-8 text regions with CVD/APCA flags, top-6 WCAG 1.4.1 collision pairs, contrast lenses, flash verdict, top-6 remediation actions; hostile/malformed payload entries are filtered, wrong versions throw). In-app, judge-gated **Copy shareable audit link** gzips the payload via `CompressionStream` → base64url → `#share=1z.<token>` (raw `1r.` fallback when unsupported), typically ~2.2 KB total. Opening a link renders a read-only **Shared audit verdict** panel above mission control — grade-colored score ring, metric tiles, color-swatched worst findings, collision duos, contrast/flash status lines, prioritized remediation with priority badges — built exclusively with `textContent` (no injection from decoded payloads), with Copy-this-link/Dismiss actions and explicit "decoded from the URL fragment, no server ever saw it" privacy copy. A `hashchange` listener re-decodes when a new share link is pasted into an open tab; corrupted tokens show a friendly error. 3 new unit tests (121 total: compaction, truncation caps + <8 KB bound, round-trip/validation/hostile-input); 4 new smoke checks (real ~2.3 KB link generated after the judge flow, fresh-page render with score ring + findings + remediation + source meta, corrupted-token error). README section + demo-panel copy documented; SW cache bumped to v36.

- [x] Ship the **Contrast X-ray loupe** — a live hover probe that makes the whole audit interactive. New pure `sampleRegionContrast()` + `XRAY_SAMPLE_DEFAULTS` in vision-core runs the text-scan's 2-means luminance split inside a small window around any probe point (flat windows honestly report `flat: true` with the mean color instead of inventing a pair) and scores the estimated fg/bg pair under all three contrast lenses at once: WCAG 2 ratio + level, 7-mode CVD projection (worst mode, failing modes, hidden-failure flag), and APCA Lc (rating + perceptual false-pass flag). In-app, a **Contrast X-ray** toggle above the source preview (enabled once the source renders, gated by `setImageControlsEnabled`) turns the cursor into a probe: a magnified pixel-loupe (48px source window → 132px canvas, crosshair, `image-rendering: pixelated`) follows the pointer with color-swatched pair chips and tone-colored verdict lines (`data-tone` pass/warn/fail incl. `hidden failure` and `perceptual risk` callouts); clicking sends the sampled pair straight into the WCAG checker (undo-safe via `clearContrastUndoState`). Pointer sampling is rAF-throttled; the loupe flips sides near edges and never traps pointer events. Fully keyboard-operable: the canvas becomes focusable while active, arrows move the probe (Shift = 1px), Enter/Space applies the pair, Esc exits (wired into the global Escape chain after the color picker), and each keyboard move announces the verdict via a `sr-only` polite live region. Reset/clear deactivates it. 3 new unit tests (124 total: three-lens scoring + minority-cluster text detection, red-on-black hidden-CVD-failure at the probe, flat/clamp/validation) and 5 new smoke checks (real pointer hover over the worst text-scan finding renders a live loupe — demo sample probes #EEF6FB on #0585C8 at 3.70:1 with CVD worst 2.64:1 tritanopia — CVD+APCA data present, real click hands the pair to the checker, Escape exits). Hooks `setXrayEnabled`/`sampleXrayAt`/`getXraySample`. README + shortcut legend updated; SW cache bumped to v38.

- [x] Add **UI component surface contrast scan (WCAG 1.4.11 Non-text Contrast)** — a fifth static-analysis surface that catches invisible controls no text lens can see: buttons/inputs whose *surface* nearly matches the page. New pure `scanComponentSurfaceContrast()` + `COMPONENT_CONTRAST_DEFAULTS` in vision-core marches a thin modal-color ring outward from each detected text region until the band's dominant color departs from the component surface (Lab ΔE ≥ 5); that ring is the adjacent color (page or drawn border) and the WCAG ratio between surface and surrounding is checked against the 3:1 non-text minimum. Honest classifications: `fail`/`pass`/`page-surface` (no boundary found within 60px — plain page text, 1.4.11 out of scope, never guessed as a failure)/`inconclusive` (image-edge clamped or invalid region). New "UI component contrast (WCAG 1.4.11)" panel under the text scan renders failing surfaces as swatch duos with ratio, needed 3:1, and boundary distance; findings embed in report JSON (`componentContrast`), CSV (3 new `component_*` columns), judge summary snapshot line, and a high-priority remediation action. Demo UI sample gained a realistic **ghost search input** (header narrowed to make room; placeholder text passes AA at 6.45:1 while its #DAE2EB surface scores 1.25:1 vs the page — flagged) with placement tuned so detector tile adjacency at the 0.5× working scale doesn't merge regions and the pinned "exactly 1 below-AA text failure" demo contract still holds. 4 new unit tests (128 total: ghost-surface fail with resolved page color, strong-surface pass, plain-page-text page-surface, validation/malformed-region handling); 6 new smoke checks (live panel finding at 1.25:1, status copy, JSON/judge-summary/CSV embeds, 1.4.11 remediation). Not yet folded into the ClearSight Score or batch/CI `auditImageAccessibility()` (candidate follow-up: add as a fifth score axis + batch column). SW cache bumped to v40.
- [x] Fold **WCAG 1.4.11 component contrast into the ClearSight Score, batch portfolio, and CI pipeline**: the shared headless audit now runs `scanComponentSurfaceContrast()`, adds a fifth independently scored axis (15 weight, renormalized when no component is evaluable), blocks release on invisible component surfaces, and carries evaluated/failing counts through batch rows, CSV, portfolio debt totals, terminal output, GitHub Markdown, and portfolio PDF summaries. The live single-screen score consumes the same component scan, production build copy is five-axis throughout, regression tests cover score validation/headless propagation, and the offline cache is v41.
- [x] Add **one-click Fix all detected failures** to the automatic text scan: every below-AA text cluster gets a source-aware accessible replacement in a single local pass, then ClearSight reruns all 12 simulations and the complete audit to prove the result. The live proof reports before→after failing-region counts, verified/follow-up status, repaired region count, and exact changed pixels; individual issue repair remains available. README updated and offline cache bumped to v39.
- [x] Close the WCAG 1.4.11 remediation loop with **one-click "Repair surface & verify"** on every failing component surface — and make the repair honest across ALL five audit axes simultaneously. New pure `planComponentSurfaceRepair()` + `applyComponentSurfaceContrastFix()` + `COMPONENT_SURFACE_FIX_DEFAULTS` in vision-core: a tiered blend-line search picks the smallest perceptual surface shift holding `minRatio` vs the adjacent page, preferring (1) surfaces where the original text survives, then (2) surface+text pairs where shifted text meets WCAG *and* an APCA Lc floor — on a light page that physics forces a genuinely dark surface with light text, because every 3:1 surface with dark WCAG-legible text is an APCA false pass — with (3) `avoidCollisionColors` steering candidates away from colors that would collapse into the screenshot's dominant palette under any of the 7 CVD matrices (no new WCAG 1.4.1 collisions), and honest best-effort fallbacks reporting real achieved ratios (`paletteSafe`, `textApcaLc`, `textAdjusted`). The repaint is cluster-gated (same conservative 0.8 rule as the text fix) inside box+boundaryDistance, delta-preserving, and counts surface/text pixels separately. In-app: repair button per finding, padded targets (3.3:1 surface, max(5, original-ratio) text, Lc 62), score baseline capture, full re-render + re-audit, and a persistent proof line (`#componentScanRepairProof`); proof survives re-renders, clears on new source/recolor/workspace reset. Demo: ghost input 1.25:1 → 11.42:1 verified, ClearSight Score 79 → 81 (+2, Grade C → B) with text/CVD/APCA/collision axes all unchanged — the axis-aware planning was tuned against real measured regressions (naive mid-gray fix: APCA false pass, −1; dark-only fix: 3 new collisions, −1). 5 new unit tests (134 total) incl. a full detect→plan→repaint→re-scan loop proving belowAA=0 and no APCA false passes post-repair; 6 new smoke checks (116 total) drive the real button end-to-end and pin the belowAA=1 demo contract, failing=0 re-scan, and a "Verified improvement" score proof. README + panel copy updated; SW cache bumped to v42.

## Architecture
### File layout
- `index.html` / `style.css` / `app.js` are deployed in `docs/` for GitHub Pages.
- `docs/js/vision-core.js` contains testable pure functions:
  - color transform matrices
  - contrast math
  - palette suggestions
- `app.js` handles UI orchestration, file loading, canvas rendering, and events.
- `tests/vision-core.test.mjs` validates core logic in Node without a browser.
- `VIDEO_SCRIPT.md` documents the 1–3 minute demo narrative required in submission.

### Runtime flow
1. User selects or generates an image.
2. App validates input and draws a scaled source image onto an internal render surface.
3. For each mode:
   - apply matrix transforms in JS (CVD modes), or
   - apply Canvas filters (low-vision modes),
   - write output into mode-specific preview canvases.
4. WCAG panel parses color inputs, computes contrast ratios, and renders recommendations.

## Static deployment contract
- Output must live under `docs/` and load correctly under a subpath using relative paths (e.g. `./app.js`, `./style.css`).
- All logic is client-only; no credentials, secrets, or paid APIs.
- Works offline after page load and never uploads images to a server.
